#include "file_system.hpp"

void
write_to_journal( int txb_bytes,
		  char* txb,
		  int ibytes,
		  char* i,
		  int bbytes,
		  char* b,
		  int Db_bytes,
		  char* Db,
		  int txe_bytes,
		  char* txe)
{
  return;
}


int checkpoint(int txe_bytes, char* txe)
{
  return 0;
}


